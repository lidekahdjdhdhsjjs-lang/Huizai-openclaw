# 🎯 记忆系统优化 - 自动提取技能

## 概述
自动从对话中提取关键信息并更新记忆系统

## 触发条件
- 每次工具执行完成
- 工具结果包含敏感信息(token, key, password等)

## 提取规则

### 1. 令牌类
```typescript
const tokenPatterns = [
  /token[:\s]+([A-Z0-9._-]+)/i,
  /api[_-]?key[:\s]+([A-Za-z0-9_-]+)/i,
  /password[:\s]+(\S+)/i,
];
```

### 2. 配置类
```typescript
const configPatterns = [
  /VPN[:\s]+(\S+)/i,
  /代理[:\s]+(\S+)/i,
  /IP[:\s]+(\S+)/i,
];
```

### 3. 任务类
```typescript
const taskPatterns = [
  /待[办做][:：]\s*(.+)/i,
  /TODO[:]\s*(.+)/i,
  /\[ \]\s*(.+)/i,
];
```

## 存储位置

| 类型 | 位置 |
|------|------|
| Token/API Key | 偏好/user-preferences.md |
| 配置变更 | MEMORY.md |
| 新任务 | 待办/active.md |
| 技能更新 | 知识库/skills.md |

## 自动更新流程

1. 监听工具结果
2. 匹配提取模式
3. 分类存储
4. 更新索引

---

*最后更新: 2026-02-23*
