## 2026-02-16 学习记录

### 今日动态

#### 12:29 - WhatsApp 连接
- WhatsApp gateway 已连接 (+8618123737466)

#### 12:30 - Connection Error 原因分析
- 之前 "Connection error" 原因：MiniMax API 限速 (rate limit)
- 已配置 GLM 模型备选：MiniMax → GLM-5 → GLM-4.7-Flash
- 现在即使 MiniMax 限速也会自动切换

#### 12:32 - 电脑睡眠设置
- 用户要求设置电脑永远不关机
- 需要 sudo 密码来修改 systemd sleep 配置
- 待用户执行

#### 12:35 - 系统状态检查
- 运行时间: 16分钟
- 负载: 0.64 (正常)
- Gateway: ✅ 运行中
- Cron 任务: 14个正常

#### 12:38 - 语音消息配置
- 收到用户语音消息 (WhatsApp)
- 已启用音频转录配置：
  - `tools.media.audio.enabled: true`
  - `messages.tts.provider: edge`
- 等待 Gateway 重启生效

### 12:45 - Connection Error 修复
**问题原因**:
- `minimax-portal` 使用 OAuth 模式，但 OAuth token 已过期/丢失
- API 返回: `authentication_error: Please carry the API secret key`

**解决方案**:
- 切换主模型: `minimax-portal/MiniMax-M2.5` → `minimax/MiniMax-M2.5`
- 使用 API key 模式而不是 OAuth
- GLM fallback 已正常工作

**修复后**:
- 主模型: minimax/MiniMax-M2.5 (API key)
- Fallback: glm/glm-5 → glm/glm-4-flash

#### 14:50 - Connection Error 根本修复
**问题根因**:
- `minimax` provider 没有配置 API key
- `minimax-portal` OAuth token 已过期
- 所有 MiniMax 相关配置都缺少有效认证

**最终解决方案**:
- 主模型改为: `glm/glm-5` (有有效 API key)
- Fallback: `glm/glm-4-flash`
- GLM API 已验证正常工作

**后续**:
- 如需使用 MiniMax，需要获取有效的 API key 并配置

#### 14:53 - MiniMax API 配置成功 ✅
**测试结果**:
- API key 在 `api.minimaxi.com` 上有效
- 主模型恢复为 MiniMax M2.5

**当前配置**:
- 主模型: `minimax/MiniMax-M2.5` (有 API key)
- Fallback: `glm/glm-5` → `glm/glm-4-flash`
- 所有 API 均已验证正常

### 待办
- [ ] 等待用户发送新语音测试转录
- [ ] 协助用户设置电脑不睡眠 (需要 sudo)
- [ ] 【新】Foundry 工具优化: message 工具 fitness 40%，需进化
- [ ] 【新】Recurring failures: exec SIGTERM (4x), edit 文本匹配 (4x)
